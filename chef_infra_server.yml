extends: substitution
message: "Use '%s' instead of '%s'"
level: error
ignorecase: false
swap:
  'Chef Server': Chef Infra Server
  '(?:[^\s]* ){0,1}Backend': Chef Backend
  '(?:[^\s]* ){0,1}Manage': Chef Manage
